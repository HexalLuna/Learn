// Chapter 2

// VARIABLES & TYPES
function fr() {
    /* 
    	Bonjour, bienvenue sur mon avancée sur le javascript (que j'ai déjà appris entièrement).
    	Bonne apprentissage à vous et j'espère vous revoir dev fullstack :').

    	Bon avant de pouvoir executer du code, je vous propose un site pour pouvoir executer vos lignes de code !
    	https://jsbin.com/juwokohedi/edit?js,console
    
    	(Tout est retrouvable dans le README.md)
    */

    /*
    	Comme presque tous les langages dynamiques, JavaScript est un langage «de type canard», et par 
    	conséquent, chaque variable est définie à l'aide du mot-clé var, et peut contenir tous les types de variables.

    	Nous pouvons définir plusieurs types de variables à utiliser dans notre code:
    */

    var myNumber = 3; // Un nombre
    var myString = "Voila ma chaîne de caracthère"; // Une chaîne de caracthère
    var myBoolean = true; // Un booléen

    /*
    	Quelques notes sur les types de variables en JavaScript:

     	- En JavaScript, le type Number peut être à la fois un nombre à virgule flottante et un entier.
     	- Les variables booléennes ne peuvent être égales qu'à true ou false. 

     	Il existe deux types plus avancés en JavaScript. Un tableau et un objet. Nous les aborderons dans des tutoriels plus avancés.
    */

    var myArray = []; // Un tableau
    var myObject = {}; // Un objet
}

function en() {
    /* 
    	Hello, welcome to my progress on javascript (which I have already learned completely).
    	Happy learning to you and I hope to see you again dev fullstack: ').

    	Good before being able to execute code, I suggest you a site to be able to execute your lines of code!
    	https://jsbin.com/juwokohedi/edit?js,console
    
    	(Everything can be found in the README.md)
    */

    /*
    	Like almost all dynamic languages, JavaScript is a "duck-like" language, and by
    	therefore, each variable is defined using the var keyword, and can contain all types of variables.

    	We can define several types of variables to use in our code:
    */

    var myNumber = 3; // A number
    var myString = "Voila ma chaîne de caracthère"; // A string
    var myBoolean = true; // Un boolean

    /*
    	Some notes on the types of variables in JavaScript:

    		- In JavaScript, the Number type can be both a floating point number and an integer.
    		- Boolean variables can only be equal to true or false.

    	There are two more advanced types in JavaScript. An array and an object. We will cover them in more advanced tutorials.
    */

    var myArray = []; // A array
    var myObject = {}; // A object
}